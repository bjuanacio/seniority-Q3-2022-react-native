import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
    bodyText: {
        paddingBottom: 16,
    },
    slider: {
        flexDirection: 'row',
    },
    sliderContainer: {
        flex: 8,
        paddingRight: 8,
    },
    sliderValueContainer: {
        flex: 1
    }
});
